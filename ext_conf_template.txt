# cat=basic/enable/010; type=boolean; label= Enable preview window: Enable frontend preview of current page under field definitions.
usePreview = 1

# cat=basic/enable/020; type=boolean; label= Enable CSS Selectors: Enable CSS selectors instead of CSS input fields for the fields and fieldsets of powermail. Options can added/modified by TSconfig (see manual for further information).
cssSelection = 1

# cat=basic/enable/030; type=text; label= Fill field with fe data: A dropdown is shown in the backend which enables to prefill any field with the value of a current logged in feuser. You can increase or decrease the fe_user field values in the dropdown menu. Just use the wanted fieldnames of the fe_user table in a comma-separated list.
feusersPrefill = name, address, telephone, fax, email, zip, city, country, www, company

# cat=basic/enable/040; type=boolean; label= Disable BE module AND DB saving: If you don't want to save powermails to database, activate this. The corresponding fields in the plugin settings and the backend module will disappear.
disableBackendModule = 0

# cat=basic/enable/050; type=boolean; label= Disable IP logging: If you don't want to save the sender IP address in the database, activate this.
disableIPlog = 0

# cat=basic/enable/060; type=boolean; label= Disable start/stop-time: If there are problems with adding fields or if you don't need this feature at all, you can disable start/stop-time for fields and fieldsets.
disableStartStop = 0

# cat=basic/enable/070; type=boolean; label= Use IRRE: Use Inline Relational Record Editing (IRRE) to enter your fieldsets and fields just by opening only one content. If you want to use the listview and enter the relation between tt_content, fieldsets and fields manually, you can disable IRRE.
useIRRE = 1

# cat=basic//080; type=text; label= Path to Unix/Linux "file" tool with tailing slash.
fileToolPath = /usr/bin/

# cat=basic/dev/1000; type=user[EXT:powermail/lib/class.tx_powermail_tsparserext.php:tx_powermail_tsparserext->displayMessage]; label=Updater
updateMessage = 0